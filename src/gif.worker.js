/**
 * Placeholder for gif.worker.js from gif.js@0.2.0.
 * The actual worker code could not be downloaded in this environment.
 * Replace this placeholder with the real worker script when available.
 */
self.onmessage = function() {
  postMessage({ error: 'gif.worker.js placeholder: worker script not available.' });
};
